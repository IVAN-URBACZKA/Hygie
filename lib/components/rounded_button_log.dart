import 'package:flutter/material.dart';

class RoundedButtonLog extends StatelessWidget {
  const RoundedButtonLog({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialButton(onPressed: () {},

    );
  }
}

